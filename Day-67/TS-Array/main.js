"use strict";
/********************************
 * TYPE ANNOTATIONS WITH ARRAYS *
 ********************************/
let fruitsList = ["Apple", "Orange", "Cherry"];
for (let i = 0; i < fruitsList.length; i++) {
    console.log(`Fruit: ${fruitsList[i].toUpperCase()}.`);
}
